using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Contemplator : Interactable
{
    public override void Interact(bool isCharge = false)
    {
        /*
        GameManager.gm.SaveGame();
        GameManager.gm.Canvas.GetComponent<UIInfo>().notiPanel.GetComponent<NotiUI>().SetNotiText("The game is saved.");
        */
    }
}
